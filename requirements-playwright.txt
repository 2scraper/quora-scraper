# The recommended engine. After installing, fetch the browser itself:
#
#   pip install -r requirements.txt -r requirements-playwright.txt
#   playwright install chromium
#
# Playwright's own bundled Chromium is enough on this site: it was served
# HTTP 200 and the full feed on every fetch that was not challenged, and the
# refusals were Cloudflare's managed challenge, which tracks the address's
# recent request rate rather than the browser build (see the README).
# `--browser-channel chrome` drives an installed Chrome if you want one anyway
# (`playwright install chrome`).
playwright>=1.40.0
